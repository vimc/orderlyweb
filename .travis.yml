language: r
cache: packages
services:
  - docker
r_packages:
  - covr
after_success:
  - Rscript -e 'covr::codecov()'
addons:
  apt:
    packages:
      - python3-pip
      - python3-setuptools
before_script:
  - pip3 install orderly-web
  - orderly-web start inst/config
dist: xenial
